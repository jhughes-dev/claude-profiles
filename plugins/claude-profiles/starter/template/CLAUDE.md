# Profile: template

Starter profile for a new development context. Replace this with guidance
specific to the scenario this branch is for — language, framework, project
conventions, and anything Claude should know when this profile is active.

`/claude-profiles:set --new <name>` branches a fresh profile from here.
